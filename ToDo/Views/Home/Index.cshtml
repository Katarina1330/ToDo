@{
    ViewBag.Title = "ListTask";
}

<!DOCTYPE html>
<html>
<head>
    <title>List Tasks</title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons"
          rel="stylesheet">
    <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.4.8/angular.min.js"></script>
    <script src="~/JavaScript/App.js"></script>
</head>
<body>
    <h1>List Tasks:</h1>
    <table ng-app="TaskApp" ng-controller="taskCtrl" border="1">
        <thead>
            <tr>
                <td><b>Id</b></td>
                <td><b>Title</b></td>
                <td><b>Created Task</b></td>
                <td><b>Description</b></td>
                <td><b>State</b></td>
                <td><b>Deadline Task</b></td>
            </tr>
        </thead>

        <tbody>
            <tr ng-repeat="task in tasks">
                <td>{{task.ID}}</td>
                <td><input type="text" ng-model="task.Title" ng-show="isEditMode"/>{{task.Title}}</td>
                <td>{{task.CreatedDate}}</td>
                <td>{{task.Description}}</td>
                <td>{{task.State}}</td>
                <td>{{task.DeadlineDate}}</td>
                
                <td><a href="" ng-show="isEditMode" ng-click="toogleEditMode(isEditMode)" ><i class="material-icons">save</i></a></td>
                <td><a href="" ng-hide="isEditMode" ng-click="toogleEditMode(isEditMode)" ><i class="material-icons">edit</i></a></td>
            </tr>
        </tbody>

    </table>

</body>
</html>